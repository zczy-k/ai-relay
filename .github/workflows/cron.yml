name: Scheduled Cron Jobs

on:
  schedule:
    # Provider health probe — daily at 00:00 UTC
    - cron: "0 0 * * *"
    # Usage aggregation — daily at 00:05 UTC
    - cron: "5 0 * * *"
  workflow_dispatch:
    inputs:
      job:
        description: "Which cron job to run"
        required: true
        type: choice
        options:
          - probe
          - usage
          - all

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Determine which jobs to run
        id: jobs
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "job=${{ inputs.job }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event.schedule }}" = "0 0 * * *" ]; then
            echo "job=probe" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event.schedule }}" = "5 0 * * *" ]; then
            echo "job=usage" >> "$GITHUB_OUTPUT"
          else
            echo "job=all" >> "$GITHUB_OUTPUT"
          fi

      - name: Validate cron configuration
        id: config
        env:
          DEPLOY_URL: ${{ secrets.DEPLOY_URL || vars.DEPLOY_URL }}
          CRON_AUTH_TOKEN: ${{ secrets.CRON_SECRET || vars.CRON_SECRET || secrets.RELAY_ADMIN_KEY || vars.RELAY_ADMIN_KEY || secrets.RELAY_API_KEY || vars.RELAY_API_KEY }}
        run: |
          set -euo pipefail

          if [ -z "${DEPLOY_URL:-}" ]; then
            echo "⚠️ DEPLOY_URL is not set; skipping cron HTTP calls."
            echo "To enable GitHub Actions cron calls, add DEPLOY_URL in Settings → Secrets and variables → Actions"
            echo "Example: https://ai-relay.pages.dev"
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [[ ! "$DEPLOY_URL" =~ ^https?:// ]]; then
            echo "❌ Error: DEPLOY_URL must include http:// or https://"
            echo "Current value: $DEPLOY_URL"
            exit 1
          fi

          if [ -z "${CRON_AUTH_TOKEN:-}" ]; then
            echo "❌ Error: no cron auth token is available"
            echo "Set CRON_SECRET, RELAY_ADMIN_KEY, or RELAY_API_KEY in repository secrets or variables"
            exit 1
          fi

          echo "base_url=${DEPLOY_URL%/}" >> "$GITHUB_OUTPUT"
          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: "Cron: Provider Health Probe"
        if: steps.config.outputs.enabled == 'true' && (steps.jobs.outputs.job == 'probe' || steps.jobs.outputs.job == 'all')
        env:
          CRON_AUTH_TOKEN: ${{ secrets.CRON_SECRET || vars.CRON_SECRET || secrets.RELAY_ADMIN_KEY || vars.RELAY_ADMIN_KEY || secrets.RELAY_API_KEY || vars.RELAY_API_KEY }}
        run: |
          set -euo pipefail
          RESPONSE=$(curl -s -o /tmp/probe.json -w "%{http_code}" \
            -H "x-cron-secret: $CRON_AUTH_TOKEN" \
            -H "Authorization: Bearer $CRON_AUTH_TOKEN" \
            "${{ steps.config.outputs.base_url }}/api/cron/probe")
          echo "Status: $RESPONSE"
          cat /tmp/probe.json | jq . 2>/dev/null || cat /tmp/probe.json
          if [ "$RESPONSE" -ge 400 ]; then
            echo "❌ Probe failed with HTTP $RESPONSE"
            exit 1
          fi

      - name: "Cron: Daily Usage Aggregation"
        if: steps.config.outputs.enabled == 'true' && (steps.jobs.outputs.job == 'usage' || steps.jobs.outputs.job == 'all')
        env:
          CRON_AUTH_TOKEN: ${{ secrets.CRON_SECRET || vars.CRON_SECRET || secrets.RELAY_ADMIN_KEY || vars.RELAY_ADMIN_KEY || secrets.RELAY_API_KEY || vars.RELAY_API_KEY }}
        run: |
          set -euo pipefail
          RESPONSE=$(curl -s -o /tmp/usage.json -w "%{http_code}" \
            -H "x-cron-secret: $CRON_AUTH_TOKEN" \
            -H "Authorization: Bearer $CRON_AUTH_TOKEN" \
            "${{ steps.config.outputs.base_url }}/api/cron/usage")
          echo "Status: $RESPONSE"
          cat /tmp/usage.json | jq . 2>/dev/null || cat /tmp/usage.json
          if [ "$RESPONSE" -ge 400 ]; then
            echo "❌ Usage aggregation failed with HTTP $RESPONSE"
            exit 1
          fi
